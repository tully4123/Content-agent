---
description: Push the current schedule to Google Drive as a Sheet for the team
---

Run the `drive-sync` skill for the current schedule. See
`.claude/skills/drive-sync/SKILL.md` for the exact steps, target folder, and
the important caveat that this creates a new file each time rather than
truly updating one in place (Drive tools available don't support in-place
edits or deletes).
